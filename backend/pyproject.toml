[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "av>=17.1.0",
    "fastapi>=0.136.3",
    "mem0ai[extras,llms]==1.0.11",
    "python-multipart>=0.0.32",
    "qwen-asr>=0.0.6",
    "uvicorn>=0.49.0",
    "vieneu>=3.0.5",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.1.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
